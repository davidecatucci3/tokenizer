# Tokenizer
⚠️ **Important:** This is not a a fast tokenizer (a tokenizer similar to the one used by the state-of-the-arte models) it's a basic tokenizer build it to show it the basic structure
of a tokenizer

## Files

## How to use it
